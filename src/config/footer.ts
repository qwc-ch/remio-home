import type { FooterConfig, Resources } from "./types";

/**
 * 底部配置
 */
export const footer: FooterConfig = {
  text: "© 2026",
  url: "",
  ICP: "",
  MPSICP: "",
  direction: "col-reverse",
  isExternal: true,
};

/**
 * 自定义资源（css / js / body html）
 */
export const resources: Resources = {
  css: [],
  js: [],
  bodyHtml: "",
};