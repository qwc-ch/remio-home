version: "3"

services:
  remio-home:
    image: kasuie/remio-home
    container_name: remio-home
    ports:
      - "3000:80"
    environment:
      - TZ=Asia/Shanghai
      # 构建时通过 --build-arg VERSION=xxx 注入版本号
    volumes:
      - /usr/local/icons:/opt/icons
    restart: unless-stopped